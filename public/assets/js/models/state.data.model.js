//public/assets/models/state.data.model.js

define([
    'underscore',
    'backbone'
], function(_, Backbone){
    var stateDataModel = Backbone.Model.extend({
        initialize: function() {

        }
    });
    return stateDataModel;
});